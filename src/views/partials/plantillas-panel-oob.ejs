<%# Actualiza la tabla fuera del target del modal mediante HTMX OOB. %>
<main class="main-content" id="plantillas-panel" hx-swap-oob="true">
  <%- include('plantillas-panel', { plantillas, total, catalogoVacio, page, totalPages, pageSize, q, estado, sort, dir, user, csrfToken }) %>
</main>
