<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<%#
  Bug real reportado por el usuario: entrar a una URL rota de más de un
  nivel (ej. /doctores/asdas) rendía esta página SIN estilos — el mismo
  problema que ya resolvieron tutor-form.ejs/laboratorio-form.ejs/agenda.ejs
  con <base href="/">, pero más agudo aquí: esta página es la ÚNICA que se
  sirve en una URL completamente arbitraria (la que el usuario haya escrito
  o roto), nunca una ruta fija — sin <base>, cualquier referencia relativa
  (sus propios <link>/<a>, y las de partials/sidebar.ejs) resuelve contra
  esa URL rota en vez de la raíz real del sitio.
%>
<base href="/">
<title>Página no encontrada | Omega Hospital Veterinario</title>
<link rel="icon" type="image/png" href="assets/imgs/icon.png">
<link rel="stylesheet" href="css/styles.css">
<link rel="stylesheet" href="css/main.css">
</head>
<body>

  <div class="app" id="app">
    <div class="top-row">
      <div class="sidebar-header">
        <img src="assets/imgs/icon.png" alt="Omega Hospital Veterinario">
        <span class="sidebar-title">Omega Vet</span>
        <button id="sidebarToggle" class="sidebar-toggle-btn" type="button" aria-label="Mostrar u ocultar menú">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
        </button>
      </div>

      <header class="topbar">
        <h1 class="topbar-title">Panel administrativo</h1>
        <a class="topbar-user" href="mi-perfil.html">
          <span class="topbar-user-avatar avatar-color-<%= user.avatar %>">
            <img src="assets/imgs/<%= user.avatar %>.png" alt="">
          </span>
          <span class="topbar-user-name"><%= user.nombre %> <%= user.apellidos %></span>
        </a>
      </header>
    </div>

    <div class="body-row">
    <div class="nav-backdrop" id="navBackdrop"></div>
    <%- include('partials/sidebar', { user, currentPage: null }) %>

    <main class="main-content">
      <div class="empty-state-panel">
        <span class="empty-state-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
        </span>
        <p class="empty-state-title">404 — Página no encontrada</p>
        <p class="empty-state-description">La dirección a la que intentaste entrar no existe.</p>
        <a class="btn-primary" href="main.html">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 12 12 4l9 8"/><path d="M5 10v10h14V10"/></svg>
          Volver al panel
        </a>
      </div>
    </main>
    </div>
  </div>

<script src="js/sidebar.js" nonce="<%= cspNonce %>"></script>

</body>
</html>
